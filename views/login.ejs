<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.7/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-yellow-400 h-screen flex items-center justify-center">
    <div class="bg-gray-700 w-72 h-96 text-center">
        
        <form action="/login" method="POST">
            <a href="/" class="text-white mr-48  "><--Voltar</a>
            <h1 class="text-2xl font-semibold text-white mb-4">Login</h1>

            <!-- Email Address -->
            <div class="mb-4">
                <label for="usr" class="block text-gray-100 text-sm font-medium mb-2">Email:</label>
                <input type="email" id="usr" name="email" class=" rounded-lg border border-gray-300 py-2 px-3 focus:outline-none focus:border-indigo-500" placeholder="Seu email" required>
            </div>
            <!-- Password -->
            <div class="mb-4">
                <label for="pwd" class="block text-gray-100 text-sm font-medium mb-2">Senha:</label>
                <input type="password" id="pwd" name="senha" class=" rounded-lg border border-gray-300 py-2 px-3 focus:outline-none focus:border-indigo-500" placeholder="Sua senha" required>
            </div>
            <!-- Submit Button -->
            <div>
                <button type="submit" class=" bg-blue-500 text-white py-2 px-4 rounded-lg hover:bg-blue-600 focus:outline-none focus:bg-blue-600">
                    Enviar
                </button>
            </div>
        </form>
    </div>
</body>
</html>
